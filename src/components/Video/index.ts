import videoModel from "./src/Video.vue";
import myComon from "./src/myComonent.vue";

import { withInstall } from "../../uitls/index";

export const BasicVideo = withInstall(videoModel);

export const BasicMyComon = withInstall(myComon);
